import { ProduitCategorieColorPipe } from './produit-categorie-color.pipe';

describe('ProduitCategorieColorPipe', () => {
  it('create an instance', () => {
    const pipe = new ProduitCategorieColorPipe();
    expect(pipe).toBeTruthy();
  });
});
